{
  "installCommand": "cd .. && npm install && cd admin-panel && npm install",
  "buildCommand": "cd ../shared-ui && npm run build && cd ../admin-panel && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
