{
  "installCommand": "cd .. && rm -rf node_modules package-lock.json && npm install && cd admin-panel && rm -rf node_modules package-lock.json && npm install",
  "buildCommand": "cd ../shared-ui && npm run build && cd ../admin-panel && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
