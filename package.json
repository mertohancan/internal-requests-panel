{
  "name": "beymen-task-approval",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "user-panel",
    "admin-panel",
    "shared-ui",
    "mock-api"
  ],
  "scripts": {
    "build:shared": "cd shared-ui && npm run build",
    "build:user": "cd user-panel && npm install && npm run build",
    "build:admin": "cd admin-panel && npm install && npm run build",
    "install:all": "npm install && cd shared-ui && npm install && cd ../user-panel && npm install && cd ../admin-panel && npm install"
  }
}
