{
  "installCommand": "cd .. && rm -rf node_modules package-lock.json && npm install && cd user-panel && rm -rf node_modules package-lock.json && npm install",
  "buildCommand": "cd ../shared-ui && npm run build && cd ../user-panel && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
