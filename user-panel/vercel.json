{
  "installCommand": "cd .. && npm install && cd user-panel && npm install",
  "buildCommand": "cd ../shared-ui && npm run build && cd ../user-panel && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
