{
  "buildCommand": "cd ../shared-ui && npm install && npm run build && cd ../user-panel && npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
