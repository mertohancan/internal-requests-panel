{
  "buildCommand": "npm install -g npm@latest && cd .. && npm install && npm run build:shared && cd user-panel && npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
